[build-system]
requires = ["setuptools>=45", "wheel", "cython>=0.29.0", "numpy>=1.19.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
